# Arquivos de projeto não devem ser servidos: a raiz do repositório é
# também a raiz do site, então README, configs e dotfiles ficariam públicos.
<IfModule mod_authz_core.c>
  <FilesMatch "^\.|\.(md|yml|yaml)$">
    Require all denied
  </FilesMatch>
</IfModule>

# Sem listagem de diretório
Options -Indexes

# Compressão
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json image/svg+xml
</IfModule>

# Cache de estáticos
<IfModule mod_expires.c>
  ExpiresActive On
  # CSS e JS com cache curto enquanto o site está em construção: o CDN da
  # Hostinger reteve uma cópia antiga por dias com o TTL de 7 dias.
  # Ao estabilizar, subir de volta e versionar a URL (style.css?v=N).
  ExpiresByType text/css            "access plus 10 minutes"
  ExpiresByType application/javascript "access plus 10 minutes"
  ExpiresByType image/jpeg          "access plus 30 days"
  ExpiresByType image/png           "access plus 30 days"
  ExpiresByType image/svg+xml       "access plus 30 days"
  ExpiresByType image/webp          "access plus 30 days"
  ExpiresByType font/woff2          "access plus 1 year"
  ExpiresByType text/html           "access plus 0 seconds"
</IfModule>

# UTF-8
AddDefaultCharset UTF-8
